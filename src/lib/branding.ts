/** Public program name (replaces legacy “Employer Network”). */
export const PROGRAM_NAME = "Community Employment Partners Network";

/** Short line for nav / tight layouts (full name is long on small screens). */
export const PROGRAM_NAME_NAV = "CE Partners Network";

/** Display name for default Resend `from` when env is unset (keep reasonably short). */
export const PROGRAM_EMAIL_FROM_LABEL = "JTSG Community Employment Partners";
